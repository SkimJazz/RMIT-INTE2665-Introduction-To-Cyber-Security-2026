# 30. vi / vim Editor Basics

Open a file:

```bash
vim filename
```

Key operations:

* Insert text → `i`
* Delete line → `dd`
* Search → `/text`
* Go to line → `:line_number`
* Quit without saving → `:q!`
* Save and quit → `:wq`
